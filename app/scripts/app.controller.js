/** 
 *   EventsApp
 * 
 *   This file includes the code that needs to be executed in a controller.
 *   Created on : 17 Jul, 2017, 11:37:25 AM
 *   Author     : Kunal Dethe
 */
(function () {
    "use strict";

    //Set the module name so that the below code will run within its scope.
    angular
            .module('eventsApp')
            .controller(controller);

    //Inject the dependencies if any.
    controller.$inject = [];

    //Contructor function
    function controller() {
        
    }
})();
